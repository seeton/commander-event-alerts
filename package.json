{
  "name": "commander-event-alerts",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit",
    "pretest": "wrangler deploy --dry-run --outdir .wrangler/test-build",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "latest",
    "miniflare": "^5.20260915.0-alpha",
    "typescript": "latest",
    "vitest": "latest",
    "wrangler": "latest"
  }
}
