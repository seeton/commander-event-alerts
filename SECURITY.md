# Security Policy

購読者アドレスはButtondownにだけ保存し、このコードやGitHub Actionsから購読者APIは呼びません。登録確認・解除・配送はButtondownが担当します。

`BUTTONDOWN_API_KEY` はGitHub Actions Secretに保存します。権限はEmailsのRead & writeとSendingだけにし、SubscribersやSettingsへのアクセスは与えません。ソースコード、Issue、Pull Request、ログ、公開アーティファクトにキーを書かないでください。

テストは通信をモックし、実在する宛先に送信しません。手動実行の初期値は送信しないpreviewです。`DELIVERY_ENABLED=true` のときだけsendが動作します。APIの応答本文や購読者情報はログへ出しません。

配信用workflowにpull_requestトリガーを加えないでください。APIキーを扱うのは管理者が承認したコードだけです。フォークや外部PRのコードにSecretsを渡さないでください。

キーが漏えいした場合はButtondown側で無効化し、GitHub Secretを更新してください。履歴から文字列を消すだけでは不十分です。公開Issueには秘密情報を投稿しないでください。
